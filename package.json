{
  "name": "my-hono-app",
  "license": "MIT",
  "scripts": {
    "dev": "cross-env NODE_ENV=dev tsx watch src/index.ts",
    "start": "cross-env NODE_ENV=prod tsx watch src/index.ts",
    "generate": "npx prisma generate --schema=src/database/schema.prisma",
    "seed": "tsx src/database/seed.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.2.3",
    "@hono/zod-validator": "^0.1.11",
    "@libsql/client": "^0.3.6",
    "@prisma/adapter-libsql": "^5.6.0",
    "@prisma/client": "^5.6.0",
    "dotenv": "^16.3.1",
    "hono": "^3.10.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "tsx": "^3.12.2"
  }
}